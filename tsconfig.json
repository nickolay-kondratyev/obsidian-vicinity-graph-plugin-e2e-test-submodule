{
	"extends": "../tsconfig.json",
	"compilerOptions": {
		// e2e runs under node (Playwright transpiles); no JSX, no DOM-global
		// assumptions beyond what page.evaluate callbacks need (lib DOM comes
		// from the root config and matches the renderer they execute in).
		"types": ["node"]
	},
	"include": ["./**/*.ts"]
}
